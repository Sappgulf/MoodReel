{
    "buildCommand": "cd moodreel && npm install && npm run build",
    "outputDirectory": "moodreel/build",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}